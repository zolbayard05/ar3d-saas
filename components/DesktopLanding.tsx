/**
 * Deleted at the user's explicit request (2026-09-05) — every landing-page
 * section component that used to render here is gone, not just unused
 * (this is git-recoverable from history if it's ever wanted back). Desktop
 * visitors (app/page.tsx branches here for any non-mobile UA) just see an
 * empty page for now.
 */
export async function DesktopLanding() {
  return <main className="min-h-dvh bg-bg" />;
}
